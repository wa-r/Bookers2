<%= render 'users/User-info', user: @user %>
<%= render 'books/New-book', book: @book %>

<h1>Users</h1>
<table>
  <thead>
    <th>image</th>
    <th>name</th>
  </thead>
  </tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= attachment_image_tag user, :profile_image, format: 'jpeg', fallback: "no_image.jpg", size: "40x40" %></td>
        <td><%= user.name %></td>
        <td><%= link_to "Show", user_path(user.id) %></td>
      </tr>
    <% end %>
  <tbody></tbody>
</table>